$('.btn-reply-<%= @post.id%>').html('<%= escape_javascript(render partial: 'public/posts/reply_area', locals: {new_post: @new_post, c: @post, topic: @topic}) %>')
$('.btn-reply-<%= @post.id%>').slideDown();