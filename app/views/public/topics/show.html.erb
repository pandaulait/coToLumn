<div class="container">
   <div class="row">
     <div class="col-12 posts-main pr-5 pl-5">
       <h2 class="question">問<%= @topic.title %></h2>
     </div>
   </div>
</div>

<div class="row">
  <div id = "demo" style="width: 100%; height: 50vh;"></div>
</div>
<% if @topic.question == true %>
  <%= render 'public/topics/normal_show', {posts: @posts, topic: @topic, new_post: @new_post} %>
<% else %>
  <%= render 'public/topics/question_show', {posts: @posts, topic: @topic, new_post: @new_post} %>
<% end %>


<script>
  var words = [
    <% @entities.each do |word| %>
    {text: '<%= word.entity %>', weight: <%= word.sum_score %>},
    <% end %>
    {text: '', weight: '0'},
  ];
  $('#demo').jQCloud(words, {
  });
</script>

